/**
 * GitHub release asset that the "Download App" buttons point to.
 * The "releases/latest" URL always redirects to the newest stable release's
 * `crrfapp.apk` asset, so this never needs to change between releases.
 */
export const APP_DOWNLOAD_URL =
  'https://github.com/Kanjo-Elkamira-Ndi/crrf-mobile-platform/releases/latest/download/crrfapp.apk';
